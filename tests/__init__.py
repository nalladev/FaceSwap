"""
Test suite for FaceSwap application.
Comprehensive testing for face detection, swapping, and utility functions.
"""

import os
import sys

# Add the parent directory to the path so we can import the main application
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
